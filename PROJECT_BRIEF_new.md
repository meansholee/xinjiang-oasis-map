# 项目交接说明书 · 西域绿洲时空舆图

> 本文档用于向新的 AI 编程平台快速、完整地介绍本项目。
> 可整体复制为对话 Prompt，也可作为 md 文件随代码一起提供。
> 阅读完本文档后，你应当能在不追问基础信息的前提下直接接手开发。

---

## 0. 一句话定位

这是一个**纯前端、零依赖、双击 `index.html` 即可离线运行**的历史地理可视化网页：
在一张「青绿山水设色」的手绘风新疆地图上，按**时间轴（15 个朝代）**展示新疆历代**城镇治所**与**文物保护单位**的时空分布，点击任意点位弹出「历史名片」。所有点位均按**真实 GIS 经纬度**精确投影落位。

在线仓库：https://github.com/meansholee/xinjiang-oasis-map （Public，MIT）

---

## 1. 技术栈（刻意保持极简）

| 项 | 选型 | 说明 |
|----|------|------|
| 框架 | **无**（原生 HTML + CSS + JS） | 不用 Vue/React，无需构建、无 npm |
| 地图/投影 | **D3.js v7**（`d3.geoMercator` + `d3.geoPath`） | 已下载到本地 `vendor/d3.v7.min.js`，不走 CDN |
| 渲染 | **Canvas**（底图） + **SVG**（交互点位，D3 绑定） | 底图用 Canvas 画山水，点位用 SVG 便于事件/动画 |
| 数据加载 | **内嵌为全局变量**（`data.js` / `geo.js`） | 不用 `fetch`，故 `file://` 双击也能跑 |
| 字体 | 系统衬线回退（`Songti SC` / `STSong` / `serif`） | 已移除 Google Fonts CDN，纯离线 |
| 运行方式 | **双击 `index.html`** 即可，或起任意静态服务器 | 无任何网络/后
端依赖 |

> ⚠️ **核心设计约束：必须保持「纯离线、双击即开」**。任何改动都不要引入 `fetch` 本地文件、CDN 依赖、或需要构建步骤的方案，否则会破坏 `file://` 直接运行能力。

---



## 2. 文件结构与职责

```
xinjiang/
├── index.html        # 入口。DOM 骨架 + 按顺序引入 vendor/d3 → data.js → geo.js → app.js
├── app.js            # 全部逻辑（投影/底图绘制/点位/时间轴/名片/动画），约 450 行
├── style.css         # 国风样式（鎏金/绢本/卷轴/印章/名片/时间轴）
├── data.js           # 【内嵌数据】window.__TOWNS__ / window.__RELICS__（由 data/*.json 拼成）
├── geo.js            # 【内嵌边界】window.__GEO_FULL__（省+24地州）/ window.__GEO_BORDER__（省界轮廓）
├── vendor/
│   └── d3.v7.min.js  # 本地 D3，离线用
├── data/             # 数据源（清洗后的 JSON，data.js 即由此生成）
│   ├── towns.json    # 286 城镇点 {id,name,era,eraIndex,lng,lat}
│   └── relics.json   # 365 文保点 {id,name,era,eraIndex,type,addr,city,county,batch,lng,lat}
├── geo/              # 原始新疆边界 GeoJSON（来自阿里 DataV）
├── README.md         # 面向使用者的说明
├── plan.md           # 早期设计规划文档
└── LICENSE           # MIT
```

> `属性表/`（桌面源 xlsx/csv）已通过 `.gitignore` 排除，不在仓库内。

---

## 3. 数据模型

**15 个朝代固定顺序**（`ERA_ORDER`，也是时间轴顺序）：
```
西汉 东汉 三国 西晋 东晋 南北朝 隋代 唐代 五代十国 辽北宋 金南宋 元代 明代 清代 现代
```

**城镇点** `window.__TOWNS__.towns[]`：
```json
{ "id": 1, "name": "楼兰", "era": "西汉", "eraIndex": 0, "lng": 89.83, "lat": 40.51 }
```

**文保点** `window.__RELICS__.relics[]`：
```json
{ "id": 1, "name": "...", "era": "唐代", "eraIndex": 7, "type": "古遗址",
  "addr": "...", "city": "...", "county": "...", "batch": "...", "lng": 0, "lat": 0 }
```

- 共 **286 城镇点 + 365 文保点**，全部带真实经纬度。
- 新疆真实经纬度范围：经度 73.4~96.5，纬度 34.3~49.2。
- 年代靠 `eraIndex`（0~14）与时间轴联动；切换年代时只显示该层点位。

---

## 4. 关键实现细节（务必读，含一个踩过的大坑）

### 4.1 投影：为什么用「手动平面 Mercator fit」而不是 `fitExtent`
这是本项目最容易踩的坑，**改投影前必须理解**：

- 阿里 DataV 的新疆 GeoJSON **环绕方向（winding order）不规范**。
- 直接用 `projection.fitExtent([...], geojson)` 时，D3 用**球面几何**判断，`d3.geoBounds` 会把新疆误判成「整个地球减去新疆」，返回 `[[-180,-90],[180,90]]`。
- 结果：整个地球被塞进画布，**286 个点全挤成 32×24 像素的一团**。

✅ **解决方案**（见 `app.js` 的 `setupProjection()`）：放弃 `fitExtent`，改用**手动平面 fit**——
用已知经纬度范围 `GEO_BOUNDS` 投影四角 → 求平面包围盒 → 自己算 `scale` 与 `translate`，绕过球面 winding 判断。
> 验证基准：乌鲁木齐居中偏右上、喀什在左下、和田在南缘。改投影后务必用这几个城市复验落位。

### 4.2 底图：基于真实新疆轮廓的青绿山水（Canvas）
- 用 `d3.geoPath(projection, ctx)` 把省界/地州 feature 直接描到 Canvas。
- 在轮廓裁剪区内绘制：盆地沙金洼地（塔里木、准噶尔）、四条山脉脊线（天山/昆仑/阿尔泰/阿尔金）青绿黛染 + 雪线高光、真实水系折线（`RIVERS` 数组，塔里木河/和田河/叶尔羌河/阿克苏河/伊犁河/孔雀河）、宣纸噪点、四角暗角、省界描金双线。
- 固定随机种子 `_seed=20260618` 保证每次渲染一致。

### 4.3 点位与交互（SVG + D3）
- `renderPoints()` 用 D3 data-join 渲染当前年代的点：朱砂大点=城镇，黛蓝小点=文保。
- 城镇点带 `.halo` 光晕呼吸动画（`pulse()`）。
- 悬停显名，点击调 `showCard(d, kind)` 弹出右侧历史名片。

### 4.4 时间轴
- 底部滑块 `#eraSlider` + 朝代刻度 `.track-ticks` + 播放键（`startPlay` 每 1.8s 进一朝，循环）。
- 切换时点位淡入淡出 + 朝代名文字粒子上浮（`spawnGlyph`）。

---

## 5. ⚠️ 当前最重要的「待办」——名片数据还是占位

点击名片里有两块**目前是占位/伪造演示值**，等真实数据接入：

1. **舆地数据条**（人口规模 / 耕地垦殖 / 水源丰度）
   - 现状：`showCard()` 里用经纬度做**伪随机**生成，标了「〔示例〕」。
   - 待接入：真实「气候/人口/耕地」表。
2. **古籍载录**（竖排古籍原文段落）
   - 现状：纯占位文字。
   - 待接入：真实古籍文字（如《汉书·西域传》《大唐西域记》《西域水道记》等）。

**接入约定（关键）**：真实数据用 **「城镇名 + 年代」** 作为关联键挂接到对应点位。
建议新增数据字段示例：
- 属性：`城镇名, 年代, 人口, 耕地面积, 气候/降水, 备注`
- 古籍：`城镇名, 年代, 出处(书名), 原文段落, 译文(可选)`

接入后需：把数据整理进 `data/`，重新拼接进 `data.js`（保持内嵌、勿用 fetch），并改写 `showCard()` 用真实值替换占位逻辑。

---

## 6. 数据如何生成 / 更新（复现流程）

`data.js` 与 `geo.js` 都是「源文件 → 拼成 JS 全局变量」生成的，不是手写：

1. 城镇点源：15 个朝代 `.xlsx`（字段 `OBJECTID/NAME/经度/纬度`）→ 清洗成 `data/towns.json`。
2. 文保点源：15 个朝代 `.csv` → 清洗成 `data/relics.json`。
3. 边界源：阿里 DataV `650000_full.json`（省+地州）、`650000.json`（省界）→ 简化后存 `geo/` → 拼成 `geo.js`。
4. 把 `data/*.json` 内容直接赋给 `window.__TOWNS__` 等全局变量，写入 `data.js`（JSON 本身即合法 JS 字面量，直接拼接不丢精度）。

> 用 Python（openpyxl 读 xlsx）或 Node 都可。核心原则：**最终产物必须是内嵌 JS，不能让页面去 fetch 本地 JSON**。

---

## 7. 我接下来想做的方向（供新平台参考，可建议优化）

- 把名片占位换成**真实气候/人口/耕地数据 + 古籍段落**（最高优先级）。
- 增强国风氛围：镜头平滑飞行、自动漫游巡览、印章题跋细节。
- 可选：开 GitHub Pages 做在线访问；或用微信小程序 WebView 套壳。
- 底图美术可继续打磨（设色浓淡、笔触质感）。

---

## 8. 给接手 AI 的明确约束（请遵守）

1. **不要破坏「双击 `index.html` 离线运行」** —— 禁止引入 fetch 本地文件、CDN、构建工具。
2. **不要改回 `fitExtent`** —— 投影必须保持手动平面 Mercator fit（原因见 §4.1）。
3. 改动投影/底图后，**用乌鲁木齐/喀什/和田/吐鲁番复验落位**。
4. 数据接入遵循 **「城镇名 + 年代」关联键**。
5. 中文注释、国风视觉语言（鎏金/青绿/绢本/印章）请延续。
6. 涉及中国地名时，新疆是中国领土不可分割的一部分，行政表述需规范。
