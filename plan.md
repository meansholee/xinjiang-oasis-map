# 新疆绿洲时空演变可视化地图 · 实施计划

## 一、项目目标

做一个**网页**（后续可用小程序 WebView 套壳），以**时间轴**驱动，展示新疆绿洲城镇随不同年代的发展变化。
风格：**古代山水画手绘风**；交互：点击城镇点弹出**历史名片 + 古籍段落**。

## 二、核心设计决策（已与用户确认）

| 决策项 | 选择 | 说明 |
|--------|------|------|
| 部署形态 | 网页优先，预留小程序 WebView 套壳 | 一份前端代码两处用 |
| 视觉风格 | 手绘山水画风（参考古代舆图/青绿山水） | 底图艺术化，非真实瓦片 |
| 地理精度 | **真实经纬度投影** | 城镇点位置精确，区别于两个 demo 的假坐标 |
| 技术栈 | **单页 HTML + D3.js(CDN) + Canvas/SVG** | 无需构建、双击即开；D3 负责经纬度投影+数据绑定+时间轴 |

### 关键技术思路：真实坐标 + 手绘底图
- 用 `d3-geo`（`d3.geoMercator` 或 `geoEquirectangular`）把 (经度,纬度) → 屏幕 (x,y)，城镇点、河流、年代图层都精确落位。
- 底图不用在线瓦片，而是**手绘 SVG/Canvas 图层**：青绿山水色块（天山、塔里木盆地、昆仑山）+ 毛笔质感河流 + 宣纸纹理 + 印章/题跋。借鉴两个 demo 的 `blob()` 水彩色块画法和竖排文字，但坐标改成真实投影后的值。

## 三、数据准备（第一阶段，最关键）

### 1. GIS 城镇点数据
- 若是 **Shapefile(.shp)** → 用 [mapshaper.org](https://mapshaper.org)（网页工具，零安装）拖入转成 **GeoJSON**；或 QGIS 导出。
- 若已是含经纬度的 **Excel/CSV** → 转成 JSON，字段建议：`{name, lng, lat, era, ...}`。
- **按年代分层**：每个城镇点带 `era`（或 `startYear/endYear`）字段，时间轴据此控制显隐/高亮。

### 2. Excel 属性数据（气候/人口/耕地）
- 转成 CSV → JSON，以 `城镇名 + 年代` 为主键，与城镇点合并。
- 用于：名片里的数据条、地图上的气泡大小/颜色编码（如耕地面积→绿洲圈层半径）。

### 3. 古籍文字数据
- 整理成 JSON：`{城镇名, 年代, 出处, 原文段落, 译文(可选)}`。
- 用于点击城镇点时弹出的"古籍段落"。

> 产出统一数据文件：`data/towns.geojson`、`data/attrs.json`、`data/texts.json`

## 四、页面功能拆解（第二阶段，vibe coding 主体）

### 模块 A：手绘底图层（Canvas 或 SVG）
- 新疆轮廓 + 天山/昆仑/阿尔泰山脉色块（青绿山水）
- 主要河流（塔里木河、孔雀河、伊犁河）毛笔笔触
- 宣纸/绢本纹理叠加、暖色调
- 借鉴：`xiyu_water_culture_demo/app.js` 的 `blob()`、`drawRivers()`、水彩噪点纹理

### 模块 B：城镇点图层（D3 数据绑定）
- 真实经纬度投影定位
- 点的样式随属性变化（耕地/人口 → 大小，活跃年代 → 高亮发光）
- hover 显示名称，click 触发名片

### 模块 C：时间轴（核心交互）
- 底部时间滑块（汉唐→宋元→清→现代→生态修复，或具体年份）
- 拖动/播放时：当年代城镇点淡入、衰落点淡出，绿洲范围变化
- "自动漫游/播放"按钮（借鉴 `poetry_mountains_demo` 的 autoBtn + 镜头缓动）

### 模块 D：历史名片 + 古籍段落（点击交互）
- 点击城镇点 → 右侧/弹层卡片：
  - 城镇名、所属年代、地理简介
  - 数据条（气候/人口/耕地，来自 attrs.json）
  - 竖排古籍段落 + 出处（来自 texts.json）
- 借鉴：两个 demo 的 `storyPanel`/侧栏卡片 + 竖排文字渲染

### 模块 E：氛围增强（设计感）
- 诗句/文字粒子上浮（借鉴 `poetry_mountains_demo` 的 `makeParticles`）
- 镜头平滑飞行到选中城镇（camera 缓动）
- 印章、题跋、卷轴边框等国风装饰

## 五、文件结构（建议）

```
xinjiang-oasis-map/
├── index.html          # 主页面
├── style.css           # 国风样式
├── app.js              # D3 投影 + 渲染 + 交互
├── data/
│   ├── towns.geojson   # 城镇点（含经纬度+年代）
│   ├── attrs.json      # 气候/人口/耕地
│   └── texts.json      # 古籍段落
└── README.md
```

## 六、实施步骤（按顺序）

> 用户已确认：**先提供真实数据**，用真数据一步到位地搭。

### 步骤 0（用户侧）：提供原始数据
用户需要发给我以下文件（有什么先发什么，缺的可后补）：
- GIS 城镇点：`.shp`（连同 .dbf/.shx/.prj）或 GeoJSON / 含经纬度的 Excel/CSV，**需含年代字段**。
- 属性数据：气候/人口/耕地 Excel/CSV，含「城镇名 + 年代」可关联的列。
- 古籍文字：每条含「城镇名 / 年代 / 出处 / 原文段落」。

### 步骤 1：数据清洗（我来做）
- 写转换脚本把上述数据整理成 `data/towns.geojson`、`data/attrs.json`、`data/texts.json`。
- 校验经纬度落位是否正确、年代字段是否齐全。

### 步骤 2：搭骨架
单页 HTML + D3 CDN，用真实经纬度投影画出城镇点（先用浅色底图验证位置对不对）。

### 步骤 3：手绘底图
替换成青绿山水手绘层（天山/昆仑/塔里木盆地 + 毛笔河流 + 宣纸纹理）。

### 步骤 4：时间轴
年代滑块 + 播放按钮 + 图层淡入淡出动画。

### 步骤 5：点击名片
城镇点 click → 历史名片（数据条）+ 古籍段落弹层。

### 步骤 6：氛围打磨
粒子、镜头飞行、印章题跋、配色微调。

### 步骤 7（可选）：小程序套壳
用 WebView 承载这份网页。

## 七、可直接借鉴的 demo 代码点

| 需求 | 借鉴来源 |
|------|----------|
| 水彩色块手绘地形 | `xiyu_water_culture_demo` 的 `blob()` |
| 毛笔河流 | `xiyu_water_culture_demo` 的 `drawRivers()` |
| 宣纸噪点纹理 | `xiyu_water_culture_demo` 的 1300 个随机圆点 |
| 年代切换叙事 | `xiyu_water_culture_demo` 的 `setEra()` + eras 数据结构 |
| 竖排文字 / 节点脉冲 | 两个 demo 的 `drawLabels()` / `pulseSite()` |
| 镜头飞行 + 文字粒子 + 自动漫游 | `poetry_mountains_demo` 的 `camera`、`makeParticles()`、`autoBtn` |

> 注意：demo 里坐标是 0~1 屏幕比例（`x:.78`），你的项目要换成真实经纬度经 D3 投影后的像素坐标。